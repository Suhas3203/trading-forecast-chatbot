[pytest]
testpaths = tests
log_cli = true
log_cli_level = INFO
addopts = -v --tb=short --html=tests/reports/report.html --self-contained-html

markers =
    smoke:       Critical path tests — run on every deploy
    unit:        Isolated unit tests with all external calls mocked
    integration: Tests hitting live external services (network required)
